<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ResBoardMapper">
    <!-- Board List -->
    <select id="boardList" resultType="ResBoardDTO">
        SELECT *
        FROM m_resboard
        <include refid="sort" />
    </select>
    <sql id="sort">
        <!-- 최신순 정렬 -->
        <if test="sort == null">
            ORDER BY r_id DESC;
        </if>
        <if test="sort == 'r_id'">
            ORDER BY r_id DESC;
        </if>

        <!-- 조회수 정렬 -->
        <if test="sort == 'r_view'">
            ORDER BY r_view DESC;
        </if>
    </sql>

    <!-- SELECT Board -->
    <select id="boardSelect" resultType="ResBoardDTO">
        SELECT *
        FROM m_resboard
        WHERE r_id = #{r_id };
    </select>

    <!-- INSERT Board -->
    <insert id="boardInsert">
        INSERT INTO m_resboard(r_uid, r_sub, r_name, r_address, r_contents)
        VALUES (#{r_uid }, #{r_sub }, #{r_name }, #{r_address }, #{r_contents });
    </insert>

    <!-- Delete Board -->
    <delete id="boardDelete">
        DELETE FROM m_resboard
        WHERE r_id = #{r_id };
    </delete>

    <!-- Update Board -->
    <update id="boardUpdate">
        UPDATE m_resboard
        SET r_sub = #{r_sub }, r_name = #{r_name }, r_address = #{r_address }, r_contents = #{r_contents }
        WHERE r_id = #{r_id };
    </update>

    <!-- Update r_view -->
    <update id="updateR_view">
        UPDATE m_resboard
        SET r_view = r_view + 1
        WHERE r_id = #{r_id };
    </update>
</mapper>